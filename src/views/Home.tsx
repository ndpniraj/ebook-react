import { FC } from "react";
import HeroSection from "../components/HeroSection";

interface Props {}

const Home: FC<Props> = () => {
  return (
    <div>
      <HeroSection />
    </div>
  );
};

export default Home;
